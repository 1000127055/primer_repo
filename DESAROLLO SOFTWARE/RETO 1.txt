CREATE TABLE CAR
	(ID NUMBER, 
	BRAND" VARCHAR2(20),
	MODEL NUMBER, 
	CATEGORY_ID NUMBER,
	PRIMARY KEY (ID)
)
SELECT * FROM CAR

BEGIN
INSERT INTO CAR (ID,BRAND,MODEL,CATEGORY_ID) 
VALUES (:id,:brand,:model,:category_id);
:status_code := 201;
END;

BEGIN
UPDATE CAR SET BRAND=:brand, MODEL=:model, CATEGORY_ID=:category_id
WHERE ID=:id;
:status_code :=201;
END;

BEGIN
DELETE FROM CAR WHERE ID=:id;
:status_code := 204;
END;